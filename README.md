# p7
mkdir
cd
gedit f1.txt
(or) touch f1.txt (create empty file)
git add .
git commit -m"Hi"
git tag v1.0 (tag can be anything)
git tag (show created tag)
git log (show file name, commit message, tag name, branch head)
